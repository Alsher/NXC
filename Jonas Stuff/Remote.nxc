int buffer;
int buffer2;

void sensors()
{
     SetSensorTouch(IN_1);
     SetSensorTouch(IN_2);
     SetSensorTouch(IN_3);
     SetSensorTouch(IN_4);
}

void var()
{
 buffer = Sensor(IN_1);
 
 if(buffer == 1)                                   // buffer/ 1 is a placeholder
 {
    buffer2 = 1;                                    // like that. buffer2 is placeholder
 }
}
void remote()
{

}


task main()
{
 sensors();
 var();
 remote();

}
